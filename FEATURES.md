# Smart Counter Island · 功能清单

> 版本：3.0.0
> 零运行时依赖 · 零原生模块 · 免安装运行库 · x64 单文件/安装包分发

## 1. 形态与状态机

| 功能 | 说明 |
|---|---|
| 灵动岛（细条） | 顶部小条：事件 emoji + 主时间单位；可黑底白字或跟随玻璃 |
| 横幅 | 胶囊形：事件名 + 主时间单位 + 更小单位（闲置自动展开） |
| 倒计时窗口 | 正方形大屏：高度 = 屏幕 1/4、宽度随文字自适应、鼠标穿透纯展示 |
| 通知弹窗 | 黑底白字：标题 + 正文（关键词红色高亮）+ 操作按钮 |
| ★ 角落卡片 | 全屏授课时可选：贴长在屏幕右上角（左上/右下内凹圆角、左下外凸、右上=屏幕角），只显示剩余时间，鼠标穿透 |
| ★ 顶部进度条 | 全屏授课时可选：贴屏幕顶边整宽（默认 16px），带进度填充，透明度可调，鼠标穿透 |
| 空态 | 没有启用中的事件时显示纯黑小条/提示 |
| 单位自动降级 | 不足 1 天→时，不足 1 小时→分，不足 1 分钟→秒（四种形态通用） |
| 日期估算方式 | 向上取整 / 四舍五入 / 向下取整 |
| 已过期事件 | 可选是否显示（显示「已过」） |
| 文言文模式 | 「距离…还有」→「距…尚有」等 |
| 多事件 | 列表 + 置顶（优先生效）+ 启用/停用；放大时可轮播 |
| 手势 | 单击收起 · 双击打开配置 · 按住上下拖拽放大/缩小 · 通知滑动收起 |
| 手动模式 | 自动 / 固定显示 / ★ 大窗口驻留（常显倒计时窗口）/ 隐藏成灵动岛 |

## 2. 玻璃与视觉

| 功能 | 说明 |
|---|---|
| 玻璃效果（7 档） | 自动 / 真实模糊 / 液态玻璃 CPU / **液态玻璃·色散版**（CPU + 固定 2px 边缘色散）/ 液态玻璃 GPU（WebGL，帧率可调）/ 模拟玻璃 / 关闭 |
| ★ 玻璃实验室 | 设置页「预览玻璃效果…」或 `--glass-lab` 打开：置顶窗口用**同一张桌面截图**并排渲染 4 张对照卡（真实模糊 / 液态玻璃现状 / ＋边缘色散 2px / ＋色散 5px＋强折射），带折射强度·带宽·渗色·高光·色散·模糊滑块；背景按需重取（取屏前自动隐藏本窗口，避免把自己拍进去），可切「实时背景」；满意点「把这组参数用到小岛」写回设置 |
| ★ 边缘色散 | R/G/B 三通道各按不同折射量位移后 screen 合成 → 玻璃边缘出现红蓝细边（真玻璃的色散）；`ui.glassAberration`（px，**默认 0 = 关闭**，此时滤镜链与旧版逐字节一致），可在实验室窗口实时调 |
| 玻璃观感参数 | `ui.refractWidth` 折射带宽(px,0=自适应) · `ui.maxRefract` 最大折射位移(px,0=自适应) · `ui.bleedOpacity` 渗色(%) · `ui.glassGlow` 高光(%) · `ui.glassAberration` 色散(px) |
| 液态玻璃（CPU） | 截屏 → SVG 位移贴图折射；中心 1:1 原画、只有边缘一圈窄折射带 + 渗色 + 高光 |
| 液态玻璃（GPU） | getDisplayMedia 屏幕视频流 + WebGL 着色器；取流失败自动回退 CPU 链路 |
| GPU 玻璃帧率 | 1–60fps（默认 30） |
| 玻璃高光强度 | 0–200%，两条链路 + CSS 表面光影统一缩放 |
| ★ GPU 观感四项 | 边缘高光强度 / 底部阴影强度 / 边缘折射强度 / 边缘折射范围（只影响 GPU 着色器） |
| 灵动岛样式 | 黑底白字 / 跟随玻璃效果 |
| 透明度（5 项独立） | 灵动岛 / 横幅 / 倒计时窗口 / ★ 角落卡片 / ★ 进度条 |
| 文字对比度自适应 | 按背景亮度切深/浅字（带滞回防闪），黑底形态恒白字 |
| 窗口位置 | 三种（★五种）形态各自独立：顶居中/顶居左/顶居右/自定义坐标 |
| 所在显示器 | 跟随鼠标 / 主屏 / 指定序号（混合 DPI 正确换算） |
| 动画 | 状态切换动画开关 + 动画帧率 20–120 |
| 暗色配置页 | 可选 |
| 截屏排除自身 | WDA_EXCLUDEFROMCAPTURE（自己的玻璃不会拍到重影；录屏里也看不到小岛） |
| ★ 传感器避让 | 摄像头（挖孔）那一块**留黑**：传感器用「相对屏幕中心偏移 / 距顶 / 直径」记录，**物理固定**；小岛黑底跟着镜头（顶边最多上移到屏幕顶），细条**左右分栏**、横幅与大卡片**长到传感器下方**；再由**独立的纯黑胶囊窗口**兜底——创建即显示、`screen-saver` 置顶、鼠标穿透、不参与截屏、**永不移动/缩放**（小岛收缩、隐藏、全屏授课时它都还在），且**落在黑底内部**（两块黑重合）。左右位置不受影响（仍按「窗口位置」设置）。预设：关闭 / 居中挖孔 / 偏左 / 偏右 / iPhone 灵动岛（三传感器），也可只改直径·偏移·距顶三个数。黑底 0.55s `cubic-bezier(.32,.72,.28,1)` 伸缩，内容 0.3s 淡入；调试模式显示传感器圆点 + 禁区虚线框 |

## 3. 智能行为

| 功能 | 说明 |
|---|---|
| 智能行为总开关 | 关闭后完全手动 |
| 闲置展开横幅 | 无操作 N 秒（默认 4s）展开 |
| 自动弹出大屏 | 横幅再闲置 N 秒（默认 15s，0=不自动） |
| 窗口最大化 | 保持灵动岛（可关） |
| ★ 全屏授课时行为 | 完全隐藏（默认）/ 右上角角落卡片 / 顶部进度条 / 保持灵动岛小条；全部鼠标穿透，退出全屏自动恢复（真全屏判定：盖住任务栏，最大化不算） |
| 悬停保护范围 | 光标在小岛附近 N px 内不因闲置展开（防跳舞 + 去抖 + 迟滞） |
| 大屏轮播 | 多个事件轮播（间隔可调） |
| 允许倒计时窗口 | 关掉后不会自动/手动弹大屏 |
| 大屏冷却 | 有输入后 60s 内不自动弹大屏 |
| 开机自启 | 直接读写 HKCU Run（★按注册表真实状态显示，含被系统禁用检测） |
| 鼠标穿透 | 全屏/展示形态自动开启（Win32 WS_EX_TRANSPARENT） |
| 始终置顶 | 每 tick 重新断言最高层级（防被弹窗抢占） |

## 4. 通知接管

| 功能 | 说明 |
|---|---|
| 接管系统通知 | 检测 ShellExperienceHost toast 与置顶小窗（QQ 等），改成灵动岛弹窗展示 |
| 通知去重 | 按窗口句柄 + 指纹去重，同一条只弹一次 |
| 通知显示时长 | 默认 8 秒（可调） |
| 关键词高亮 | 正文关键词红色高亮 |
| 提醒抖动 | 提醒类通知正文 0.2s 高频抖动（可关） |
| 免打扰至下课 | 按时间表当前课时下课时间计算（无课表/非课时 → 45 分钟）；期间不弹系统通知 |
| 操作按钮 | 「免打扰至下课」/「取消关机」等自定义按钮 |

## 5. 时间表 ★

| 功能 | 说明 |
|---|---|
| 启用时间表 | 总开关 |
| 节次 + 科目 | 每节课自动编号（第 N 节）+ 可填科目名，提醒里会用上 |
| 几周一休 | 0 = 每周相同，2 = 单双周（休息周可少排课） |
| 跨午夜 | 支持 23:30→00:01 这类时段（下课提醒会在次日正确触发） |
| ★ 快速排课 | 第一节开始 + 每节时长 + 课间 + 节数 → 一键生成（可只填工作日） |
| ★ 导入/导出 | 整份课表导出 JSON 分享；粘贴导入 |
| 批量复制 | 复制此天 / 复制此周 / 复制到所有周（两次点击） |
| ★ 预览行 | 实时显示「现在 10:02 · 正在上：第 2 节 数学 · 下一节：第 3 节 英语 10:50（48 分钟后）」 |

## 6. 提醒

| 功能 | 说明 |
|---|---|
| ★ 课堂提醒（时间表驱动） | 课前 N 分钟（默认 3）/ 上课 / 下课前 N 分钟（默认 2）/ 下课（默认关） |
| ★ 文案模板 | `{min}` `{n}` `{subject}` `{label}` `{time}` 占位符，配置页实时预览 |
| ★ 提醒去重 | 同一天同一节同一触发点只提醒一次 |
| ★ 与桌宠联动 | 课堂提醒 = 通知弹窗 + 桌宠出面念一遍（可关） |
| 定时提醒 | 手配条目：到点弹提醒，支持每天/仅一次/指定星期几 |
| 定时关机 | 「关机前 N 分钟提醒」+ 到点关机；弹窗可「取消关机」，取消后当天不再触发 |
| 运行命令 | 到点执行指定命令 |
| 检查频率 | 20 秒轮询，匹配到分钟；同一分钟不重复触发 |
| 免打扰 | 课堂提醒不受免打扰影响（教学刚需）；系统通知受免打扰抑制 |

## 7. 壁纸

| 功能 | 说明 |
|---|---|
| 启用壁纸 | 总开关 |
| 底图来源 | 我的图片文件夹（推荐）/ 内置渐变底纹（8 套兜底） |
| 图片文件夹 | 默认 `%APPDATA%\SmartCounterIsland\wallpapers\source`（自带说明文件） |
| 填充方式 | 铺满裁剪（cover）/ 完整显示（contain） |
| 图片压暗 | 0–70 |
| 柔和底衬 | 语录后面垫一层暗色渐变（亮天空图也能看清字） |
| 励志语 | 内置 40 条 + 自定义（一行一条，追加） |
| 文字位置/大小 | 居中/左下/右下/顶部居中；40–250% |
| 落款 / 小字 | 右下角落款（学校班级）、左下角小字 |
| 轮换频率 | 每天 / 12h / 6h / 2h / 1h / 30min / 10min / 5min / 每次启动 |
| 轮换顺序 | 按文件名顺序 / 倒序 / 随机（不与上一张重复） |
| 立即更换 / 预览 / 恢复原壁纸 | 预览只回传 base64 不改桌面；原壁纸路径自动记录 |
| 文件保留 | 只保留最近 12 张生成的壁纸 |

## 8. 桌宠 ★

| 功能 | 说明 |
|---|---|
| 启用桌宠 | 总开关（默认关） |
| 舞台窗口 | 独立透明置顶窗口，桌宠在窗口内 60fps 走动，走到边缘整窗平移一格 |
| ★ 移动逻辑 | 按真实时间步进（帧率无关，不再隔帧减速抖动）；贴到屏幕边缘时按主进程回报的**实际**位移补偿并转身停顿（不再原地抽搐）；按窗口所在显示器的工作区限制（副屏不会被拉回主屏）；位置写盘做 1.2s 防抖（走路不再每一步都写设置）+ 退出前兜底落盘 |
| 逐像素命中 | 只有指针在桌宠身上才可点击，空白处穿透桌面 |
| 拖动搬家 | 位置持久化 |
| 大小 / 速度 / 透明度 | 50–200% / 20–400% / 20–100% |
| 行为 | 发呆 / 走动 / 睡觉 / 说话 / ★ 上课静默；全屏授课彻底隐藏 |
| 交互 | 单击弹说话气泡（4 个快捷问题 + 输入框），Esc 收起 |
| 素材包 | `pet.json` + 图片；三种写法：帧序列 / 精灵图（sheet+cols）/ 动图（GIF·WebP）；支持 png/jpg/webp/gif/bmp/svg，单文件 ≤3MB |
| 素材包管理 | 打开文件夹 / 浏览 / ★ 生成示例素材包（含说明文件）/ 重新加载；缺素材自动回落内置占位小人 |
| 预置问答 | 一行一条「关键词 \| 回答」，离线可用、零成本 |
| AI 答疑 | OpenAI 兼容接口（DeepSeek/通义/智谱/豆包…），流式逐字 |
| ★ 永不开启思考 | 请求体固定 `thinking:{type:'disabled'}`；模型名含 reasoner/thinking/r1/o1/o3 强制改回 `deepseek-chat` |
| 配额与缓存 | 每分钟/每天上限（默认 6/200）+ 同问题 10 分钟缓存 + 单并发 |
| 断网降级 | 无 Key/断网/超配额 → 用预置问答并在气泡说明 |
| 护栏 | 回答字数上限（默认 120，超出截断）、敏感词拦截、去 Markdown/代码块 |
| 课堂纪律 | 上课时间拒绝闲聊（只答课表/倒计时）；免打扰期间不主动说话 |
| 状态与自检 | 模型/Key/预置条数/提问次数/平均首字；连接自检 + 让它说一句 |
| 提问日志 | `<userData>/pet-ai-log.jsonl`（问题、回答、延迟、token） |

## 9. 天气 ★

| 功能 | 说明 |
|---|---|
| 数据源 | Open-Meteo（免密钥）：地理编码 + 当前天气 + 逐小时 + 7 天预报，`timezone=auto`，零运行时依赖（Node 内置 https） |
| 城市 | 手填城市名 → 自动地理编码并缓存经纬度；「解析城市」先确认「城市 · 省/州 · 国家」；「自动定位」按公网 IP 一键填（仅点击时执行） |
| 刷新与降级 | 默认 30 分钟（最小 10）；8 秒超时；失败保留旧数据 + 状态行标注「数据已过期」，退避 3 倍间隔，绝不弹崩溃框 |
| 岛内显示 | **常驻**：细条紧凑 chip（只图标 + 温度）+ 横幅 / 大卡片内联（图标 + 温度 + 文案）；挖孔屏下细条自动加宽出天气槽，两瓣仍紧贴禁区；可选「仅横幅·大卡片」或「不显示」 |
| ★ 天气位置 | 细条上放哪儿：右端（默认）/ 左端 / **盖板上**（画在传感器盖板窗口里 —— 盖板是独立置顶窗口，天气由它自己渲染；细条让位不占宽。会与摄像头区域重叠，摄像头不参与使用时再选） |
| ★ 盖板自定义内容 | 中间盖板上画**自定义文字**：模板 + 程序变量（16 个：`{emoji}{name}{days}{unit}{sub}{date}{count}{time}{sec}{week}{subject}{next}{min}{temp}{weather}{city}`）；配置页变量胶囊一键插入 + 按真实数据实时预览；未知变量渲染成空串；模板非空时不再自动画天气 chip（只把宽度给文字）；纯函数模块 `main/cover-text.js` 可直接自检 |
| ★ 图标动画 | 晴（光线旋转）/ 多云·阴（云飘）/ 雨（雨丝）/ 雪（雪花）/ 雷（闪）/ 雾（流动）；纯 CSS transform/opacity，强度 0–200，尊重 `prefers-reduced-motion` |
| ★ 提醒特效 | 天气提醒弹出时整条岛按天气播特效（雨丝 / 雪花 / 晴空 / 云影 / 雷闪 / 雾带），层数按强度缩放 |
| ★ 指定时间提醒 | 独立规则表：时间 + 星期（每天 / 仅一次 / 工作日）+ 内容（当前天气 / 今日概况 / 明日预报 / 降雨提醒 / 降温·高温） |
| 提醒去重 | 20 秒轮询匹配到分钟，同一分钟不重复；「仅一次」触发后自删 |
| ★ 阈值可调 | 高温阈值（默认 35℃）、降温阈值（默认 8℃）、降雨前瞻（默认 6 小时）、关键词高亮（默认 雨/雪/大风/降温/高温） |
| 「试一条」 | 配置页一键弹示例，确认文案与动画 |

## 10. 工程与运维

| 功能 | 说明 |
|---|---|
| 系统探针 | 常驻 PowerShell 子进程：前台窗口矩形/类名/全屏判定/光标/最后输入/通知枚举；按活跃度分档采样（350/700/1050ms） |
| 命令通道 | 圆角区域（SetWindowRgn）、截屏排除、鼠标穿透、桌面壁纸，全部走 `%TEMP%` 命令文件 |
| 配置存储 | `%APPDATA%\SmartCounterIsland\settings.json`，旧版本自动迁移；`SCI_USER_DATA` 可隔离 |
| 配置页 | ★ 10 个标签页，左侧分组导航（教学 / 外观 / 行为与系统）+ 顶栏设置搜索；★ 液态玻璃外观：无边框窗口 + Win11 亚克力（真实桌面模糊）+ 半透明玻璃面板（backdrop-filter 22px）+ 高光描边，窄窗自动回落单列 |
| 托盘 | 单击显隐 · 双击配置 · 右键：4 种手动模式 + 立即放大 + 收起 + 更换/恢复壁纸 + 打开配置 + 退出 |
| 诊断参数 | `--test` 274 项断言 · `--perf` · `--shot` · `--shot-strip` · `--shot-init`（启动初始化快照）· `--shot-config`（设置页逐页截图）· `--shot-lab`（玻璃实验室截图）· `--glass-lab`（打开玻璃实验室窗口）· `--shot-weather`（天气 chip/特效/盖板截图）· `--gpu` · `--gl-lag` · `--smoke` · `--demo-notify` · `--diag[=liquid\|webgl]` |
| 开发脚本 | `measure-perf.ps1`（按进程角色拆分 CPU 的 A/B）· `test-probe.ps1` · `gen-icons.js` · `rcedit-exe.js` · `gh-release.js` · `git-push-api.js` · `toast-test.js` · `send-test-toast.cmd` |
| 打包 | NSIS 安装包 + 便携版（约 71MB）；asar 打包；只保留 zh-CN/en-US 语言包；compression maximum |
| 性能取舍 | 舞台窗口方案（避免逐帧移动窗口）、GPU 模式主进程不再截屏、网页帧率节流用高精度时钟、截屏小图快路径 |
| 无依赖 | 运行时不装任何 npm 包、不带原生模块，纯 Electron + PowerShell + WebGL/SVG |
