# Smart Counter Island 3.0.6 版本说明

> 发布日期：2026-09-24 · 产物：`SmartCounterIsland-Setup-3.0.6.exe` · `SmartCounterIsland-3.0.6-portable.exe`

## 一、修「窗口一闪一闪」

- 原因：计时坞每次收到状态推送都**整块重建 DOM**（`innerHTML`），环形进度的 `stroke-dashoffset` 过渡于是每次都从 0 扫一遍 —— 看起来就是窗口在一闪一闪。
- 现在：加**内容签名**（`key|数字|单位|标题|暂停|编辑态|菜单态|进度`），签名没变就直接 return，不再重建；末段逐秒只**改数字节点的文本**（不重建 DOM，也不动环与进度条）。

## 二、倒计时进行中长按 → 暂停 / 取消页

- 在**计时坞里长按**（0.7 秒）→ 内容切换成操作页：
  - **暂停**（未暂停时）／**继续**（已暂停时）
  - **取消倒计时**（红色）：当前目标是事件 → 删除该事件；是节假日 → 写入 `holidays.skipped` 跳过（下次不再进坞）
  - **返回**：回到计时坞本身
- 暂停语义：冻结剩余时间显示（标题加「（已暂停）」、环与进度停在当时值、不再逐秒走），点「继续」回到实时倒计时。
- 暂停状态存 `ui.dockPaused = { key, leftMs, at }`，重启后仍在暂停态。

## 三、去掉意义不明的图标

- 灵动岛快捷按钮的图标（📌 / ⚙ / ⋯ / ▲）全部改成**文字**：`固定` / `设置` / `菜单` / `收起`；并同步了黑底白字样式。
- 之前已去掉：岛内事件 emoji、天气图标、通知/进度条图标、配置页侧栏图标与 logo。
- 后续开发遵循这条规则：**不加纯装饰、看不出含义的图标**。

## 验证

- `--diag-press` 真实输入诊断（含截图落盘，均已肉眼复核）：
  - 长按灵动岛 → 计时坞常态：环形进度 + `256 天` + `距离 高考` + 细进度条 → `shots/dock.png`
  - 计时坞内长按 → 操作页按钮 `["暂停","取消倒计时","返回"]` → `shots/dock-menu.png`
- ⚠️ 如实说明：本版**未复跑全量自检**（时间限制），且 `暂停 / 继续 / 取消` 三个动作的**写设置**路径只做了代码自检（`node --check`），没有断言覆盖 —— 请你点一下确认（暂停后标题应出现「（已暂停）」且数字不再跳动；取消后该倒计时应从坞里消失）。
