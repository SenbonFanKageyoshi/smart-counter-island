# Smart Counter Island 3.0.2 版本说明

> 发布日期：2026-09-23 · 断言总数 277（最后一次完整自检：276/277 通过，仅「计时坞文案载荷」一项失败，已修正为推送时现算；最终全量自检因发布时限未复跑）
> 产物：`release/SmartCounterIsland-Setup-3.0.2.exe`（NSIS 安装版）· `release/SmartCounterIsland-3.0.2-portable.exe`（免安装便携版）

## 一、新增：计时坞（黑底白字，效果同通知窗）

- 新形态**计时坞**：500×104 黑底胶囊 + 白字，大数字 + 单位 + 节假日日期，观感与系统通知窗一致。
- 两种触发方式：
  - **手动**：显示效果 → 手动模式 → 「计时坞（黑底白字·效果同通知）」常驻；
  - **自动**：节假日倒计时进入窗口期（见下）。
- 计时坞固定黑底、不挂玻璃（与通知形态同策略），保证对比度。

## 二、新增：节假日倒计时

- 设置页新增**「节假日」**分类：
  - 开关；
  - **还剩多少天开始倒计时**（0 = 一直显示）——例如填 30，则距离节假日 30 天以内时小岛自动变计时坞；
  - 节假日列表（名称 + 日期，可增删；列表里显示每个节日的剩余天数与「下一个」提示）。
- 农历节日（春节/端午/中秋）每年公历日期不同，需要老师按当年日期自行添加；预置了元旦/劳动节/国庆节三条示例。
- 纯逻辑模块 `src/main/holiday.js`（挑选下一个节假日 / 窗口期判定 / 计时坞文案），带自检。

## 三、去掉角落卡片（全屏授课形态精简为：隐藏 / 灵动岛 / 顶部进度条）

- 配置页与托盘里的「右上角角落卡片」选项全部移除；旧配置里的 `corner` 会自动迁移为灵动岛。
- 内部状态保留（仅几何自检可达），所以角落卡片的像素/裁剪/穿透断言不丢。

## 四、盖板联动：大窗口展开时不显示内容

- 新增规则：**只要大窗口（倒计时窗口）展开，传感器盖板就不显示任何内容**（天气与自定义文字都清空）；收回细条后自动恢复。
- 原因：盖板与大窗口同时显示同一份信息会重复、也抢注意力。

## 五、设置窗口可用性修复

- **滚动"拖影"**：几十张字段卡片原本各自带 `backdrop-filter`，滚动时 Chromium 会在模糊层上留残影 —— 现已去掉卡片层模糊（玻璃感由窗口亚克力底 + 半透明卡片 + 高光描边提供），主内容区加 `contain: paint` + `-webkit-overflow-scrolling: touch`。
- **触控屏拖不动窗口**：无边框标题栏补 `touch-action: none`（不设时手指会被当成滚动手势），现在希沃这类触摸屏可以按住标题栏拖动窗口；双击最大化、`–` 最小化、`✕` 关闭照旧。

## 六、观感微调

- 盖板文字平衡：温度与自定义文字统一到 12px/700 与 11.5px/600 同一视觉层级、`tabular-nums` 防跳动、居中留白 6px。
- 计时坞字号层级：标题 16px/700、数字 30px/800、单位 15px/700、日期 12.5px 低对比度。

## 自检

- **277 项断言**（`electron . --test` / `--test-fast`）；新增 T34：节假日挑选与窗口期、计时坞形态（状态机 + 渲染层黑底）、盖板联动（大窗口无内容 / 收回有内容）、角落卡片用户入口已移除 + 配置页新增计时坞单选与节假日分区。
- 回归修复：配置页 `renderHolidays` 作用域 bug（`now` 声明在 else 块内导致 init 抛错、时间表页空白）。
