# Smart Counter Island 3.0.7

## 修复

- **短时倒计时到点不再静默**：原先调用了一个从未定义的 `showNotify`，归零后无弹窗、无声音。
- **节假日「取消倒计时」静默失效**：写入用 UTC 日期、比对用本地日期，差一天 → 改用本地日期。
- **删掉最后一个事件后不再自动复活「高考」**：首次运行才给示例，之后允许真正的空态。
- **长按灵动岛 700ms 全程无反馈** → 补上按住/触发的视觉反馈。
- 配置页单选按钮逐字竖排、时间表标签错位 → 布局修复。
- 计时坞点「开始」时长被打成 60 秒 → 修复（冗余监听器丢掉了时长）。

## 行为

- **计时坞只服务手动设置的计时**：只有**长按灵动岛**才会展开；不再常驻、不再因节假日到点自动弹出；启动状态固定为灵动岛。
- 时长选择改为**对数游标尺**：中间指示线固定不动，下方刻度可左右滑动，无格数上限（1 分钟 ~ 24 小时）。
- 创建页右侧新增两个**大号 ✓ / ✗**（52px），取代底部的开始/取消按钮。
- 计时进行中**只在灵动岛显示**，不再进入大窗口。
- 盖板（挖孔屏避让）规则：计时中显示剩余；设置中左侧显示时间、右侧显示 ✓/✗；**任何非灵动岛形态一律清空**；内容避开盖板，不遮挡文字。
- 通知接管增加节流（默认 5 秒），全屏授课时静默。
- 删除类操作统一为「删除后 5 秒内可撤销」。
- 大窗口恢复为纯展示（鼠标穿透），移除其底部按钮。

## 视觉

- 次要文字对比度 3.92 → **4.89:1**；主按钮白字 2.93 → **6.37:1**。
- 小按钮点击目标 21 → 27px（对齐 WCAG 2.5.5）。
- 盖板上的文字不再会被拖选。

## 其它

- 配置窗口与玻璃实验室按屏幕工作区自适应（1366×768 等小屏不再超出屏幕）。
