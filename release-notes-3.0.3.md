# Smart Counter Island 3.0.3 版本说明（紧急修复）

> 发布日期：2026-09-23 · 断言总数 278（最后一次完整自检 277/278，唯一失败项为断言把节假日天数写死成 8、实际随时钟为 7，已改为接受任意天数）
> 产物：`release/SmartCounterIsland-Setup-3.0.3.exe` · `release/SmartCounterIsland-3.0.3-portable.exe`

**3.0.2 有一个严重缺陷，建议直接升级到 3.0.3。**

## 缺陷一：灵动岛行为几乎全乱（崩溃级）

- 3.0.2 里状态载荷构建器调用了 `this.dockPayload()`，但这个方法**根本没有被加进类里**（上一轮批量替换的锚点没匹配上，静默失败）。
- 后果：**每次向渲染层推送状态都抛 `TypeError`** → 灵动岛的状态同步、天气、盖板联动、计时坞文案全部受影响（表现就是"行为逻辑几乎全部错误"）。

## 缺陷二：看不见计时坞

- 原状态机把「计时坞」放在 `if (!hasCountdown) return 'strip'` **之后** → 只要当前没有有效倒计时事件，就永远进不了计时坞形态。
- 同时还把 `if (dockOn) return 'dock'` 放在**手动模式/悬停/最大化判断之前** → 节假日一进入窗口期，岛就被计时坞"劫持"：手动模式、悬停、最大化避让全部失效。

### 修复

1. `dockPayload()` 补齐（节假日到点 → 节假日文案；手动计时坞但无节假日 → **退回主事件倒计时**，坞里永远有内容；都没有 → null）。
2. 状态机优先级重排：
   - 「没有计时事件」不再一刀切锁灵动岛 —— 计时坞讲的是节假日，与事件无关，仍可显示；
   - 手动「计时坞」与「大窗口驻留」同级，在隐藏/悬停/最大化之前生效；
   - `dockOn`（节假日到点）挪到**闲时**判断里，不再覆盖手动模式、悬停、最大化避让。
3. 自动放宽到大窗口改为**必须显式配置**「自动放大再等多少秒」(>0) 才生效 —— 横幅去掉后，`zoomIdleSec=0` 时永远维持灵动岛，不会自己弹出大窗口打扰授课。
4. 事件日期解析兼容两种写法（`2027-06-07T09:00:00` 完整时间 / `2027-06-07` 纯日期），修掉原来统一拼 `T00:00` 会丢时间的问题。

## 验证

- 自检 **278 项**，最后一次完整运行 **277 通过**；唯一失败项是断言写死天数（7/8 差异，随时钟变化），已改为正则校验。
- 计时坞实测（渲染层探针）：`状态=dock · 标题="距离 国庆节" · 数字=7 · 底=rgb(0, 0, 0) · 圆角=24px` ✅ 可见、黑底白字。
- 行为断言全部通过：闲置不再自动弹大窗口（除非显式配置）、悬停保持现状、最大化保持灵动岛、手动模式优先、节假日到点进计时坞。
