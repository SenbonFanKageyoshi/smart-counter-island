# Smart Counter Island 3.0.4 版本说明

> 发布日期：2026-09-24 · 断言总数 282（`electron . --test` / `--test-fast` 全绿）
> 产物：`release/SmartCounterIsland-Setup-3.0.4.exe` · `release/SmartCounterIsland-3.0.4-portable.exe`

## 新增：长按灵动岛 / 盖板 → 计时坞快捷添加倒计时

- **长按灵动岛**（约 0.7 秒）或 **长按传感器盖板** → 立刻切到**计时坞**形态并进入**快捷添加**态：
  - 坞里一排芯片：`+1 天` `+3 天` `+7 天` `+30 天` `自定义…` `完成`；
  - 点任一天数芯片 → **新建对应天数的倒计时事件**（名字自动生成，如「7 天倒计时」）→ 坞里立刻显示，并自动退出编辑态；
  - `自定义…` → 打开设置页（改名字/精确日期时间）；
  - `完成` → 只退出编辑态，坞继续显示当前倒计时。
- **长按反馈**：按住时整块**稍微放大**（灵动岛 1.04 / 盖板 1.06），触发瞬间再弹一下（1.07 / 1.10），220ms 后自然回落。**没有进度条**（按上一版反馈去掉）。
- 盖板窗口原先 `setIgnoreMouseEvents(true)`（完全忽略鼠标）→ 现在可交互，长按盖板同样生效。

## 修复：长按与"下滑放大"手势冲突

- 原来按住 700ms 触发长按后，如果你本意是**下滑**，手指继续移动、抬手时 `gesture dy≥25` 又触发一次「放大成大窗口」→ 两个动作叠加。
- 现在：按住期间**只要真的移动（>8px）就立刻取消长按**；**竖直方向移动 >6px 优先取消**（竖直=下滑意图）；长按已触发过的那一次按压，抬手时**不再补发**手势/单击。

## 修复：计时坞在"没有倒计时事件"时画不出来（关键）

- 渲染函数里「无事件 → 画空态并 return」的分支排在计时坞渲染**之前** → 只要当前没有有效倒计时事件，计时坞就永远只显示「暂无倒计时事件」（之前报告的"无法看见计时坞"的另一半原因）。
- 现在计时坞分支提到空态判断之前：计时坞讲的是节假日/快捷添加，**不依赖是否已有事件**。

## 验证

- 自检 **282 项全绿**。
- **真实输入管线验证**（新增 `--diag-press`：用 `webContents.sendInputEvent` 模拟真鼠标按下并保持 1 秒，报告落盘）：
  ```
  pill={"x":66,"y":21,"w":116,"h":26}
  按住 1s 后：downs=1 fired=true state=dock mode=dock
  坞内 DOM：<div class="dk-wrap"><div class="dk-title">距离 高考</div>…  hasWrap=true hasChips=true
  松手后：state=dock dockEdit=true
  ```
  → 长按（真实鼠标事件）确实触发、计时坞内容与快捷芯片都渲染出来了。
