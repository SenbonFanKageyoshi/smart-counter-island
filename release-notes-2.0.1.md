## 本次更新（2.0.1）

### 1. 液态玻璃（GPU 加速）新增观感调节
配置页「高级 → GPU 液态玻璃观感」新增四项（100% = 默认值）：

| 选项 | 作用 |
|---|---|
| 边缘高光强度 | 边缘那圈高光的强度，0 = 完全无边缘高光 |
| 底部阴影强度 | 玻璃下部渐暗（厚度感），0 = 不压暗 |
| 边缘折射强度 | 边缘折射的最大位移量 |
| 边缘折射范围 | 边缘折射的带宽 |

四项只影响 GPU 着色器这一条链路，CPU 链路（液态玻璃·兼容）沿用原数值不变。

### 2. GPU 玻璃「跟手」优化
- **GPU 模式下主进程不再截屏**：原来每次状态切换/窗口变化都会在主进程抓一次屏，是大窗口卡顿的主要来源。
- **几何变化立即重绘**：窗口移动/缩放后不再等下一个采集帧，改用当前帧立刻重画。
- **大窗口宽度微调不再走窗口动画**：倒计时窗口内容宽度变化时直接调整尺寸，玻璃不会被隐藏 150ms。
- **帧率节流改用高精度时钟**：Windows 上 `Date.now()` 精度约 15.6ms，会把 60fps 量化成约 31ms 一跳并抖动。
- 新增诊断：`SmartCounterIsland.exe --gl-lag` 输出采集帧率、节流间隔、每帧上传/着色耗时、实际绘制间隔、主进程截屏次数，直接定位瓶颈。

### 3. 壁纸
- **壁纸不再显示倒计时**（只保留励志语 + 可选落款/小字）。
- 新增「**多长时间轮换一次**」：每天（默认）/ 12 / 6 / 2 小时 / 1 小时 / 30 / 10 / 5 分钟 / 每次启动换一次。
- 新增「**壁纸轮换顺序**」：按文件名顺序（默认）/ 倒序 / 随机（不与上一张重复）；励志语跟着一起换。
- 「预览」现在等于「点立即更换会得到的那一张」；状态栏显示最近更换与下次轮换时间。
- 未到轮换时间不会改动壁纸，你手动换的壁纸不会被抢。

### 4. 开机自启状态修正
- 勾选框改为按 **Windows 注册表真实状态**显示（原来显示的是设置里的期望值，与系统实际状态可能不一致）。
- 直接读写 `HKCU\...\CurrentVersion\Run`，顺带读取 `StartupApproved` 判断是否被「任务管理器 → 启动」禁用；注册表项指向别的 exe 时会写明「当前指向」。
- 启动时自动对齐设置与注册表（补写 / 删除），便携版写入原始 exe 路径。

### 5. 其它
- 配置页与 README 删除了冗余说明文字。
- 修复自检偶发抖动：隔离测试目录按 PID 命名，PID 复用会让上一次运行的配置污染下一次自检（现已每次自检前清空）。

---

### 安装 / 使用
- 安装版：`SmartCounterIsland-Setup-2.0.1.exe`
- 免安装版：`SmartCounterIsland-2.0.1-portable.exe`
- 自测：两种都可加 `--test` 跑 155 项自动化断言（本版本 155 PASS / 0 FAIL）
