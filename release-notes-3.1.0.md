# Smart Counter Island 3.1.0

## 天气

- **修好"天气彻底消失"**：盖板显示天气原先还要求 `pos === 'cover'`，而老配置里 `pos` 是旧默认值 `right` —— 结果盖板不显示、岛上也不显示。现在只要启用天气、且没关掉，就画在盖板上。
- 天气**只在盖板上显示**：灵动岛任何形态（含横幅）都不再画天气 chip，细条也不再为它预留那 88px 宽度。

## 设置页

- 移除「天气位置（细条上放哪儿）」选项 —— 位置已固定为盖板。
- 「岛内显示」三态收敛为两态：**显示天气（画在盖板上）/ 关闭天气**；旧配置里的 `banner` 自动归到 always。
- 移除「横幅」相关的死选项：**横幅形态已不可达**（`decideState` 不会返回 `expanded`）。
  删掉的是「横幅」位置模式 + X/Y 坐标（在配置脚本里连接线都没有，长期是僵尸 UI）与「横幅」透明度滑杆。
- 「置顶」按钮提示词里的"灵动岛和横幅" → "灵动岛上"。

## 节假日

- **恢复：节假日到点自动展开计时坞**（这一条此前被一并去掉了）。
  坞里给两个动作：**「知道了」**（收起，同一节日本次不再自动弹）、**「本次跳过」**（写入 `holidays.skipped`，之后不再提醒这一个）。
- 触发条件只避开真正抢注意力的通知 / 大窗口形态；触发前先复位到灵动岛，避免从中间态直接跳坞。
- **手动计时那条路不受影响：仍然只有长按灵动岛才会开坞。**

## 桌宠

- **暂停使用**：功能尚未开发完成。配置页「桌宠」整页灰置并不可交互，顶部给出说明；
  同时强制关闭 `pet.enabled`，保证它不只是"看着灰"而是真的不运行。
  设置项保留，待后续版本恢复。

## 其它

- 大窗口恢复为纯展示（鼠标穿透），移除其底部按钮。
- 自检 305 项全过。
