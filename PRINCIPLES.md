# Smart Counter Island · 工作与产品原则

> 这份清单是本次会话里**用返工和事故换来的**，每条都附了当时的证据。
> 目的：下次接手（人或 AI）不必再踩一遍。

---

## 一、工作原则（怎么干活）

### 1. 逐行读、逐行改；不做脚本化的文本片段替换
- 事故：一次按"片段替换"删按钮，把监听器切在半截 `});` 上 —— 语法的错好查，**没语法错的那次更危险**：`#buttons` 元素删了、渲染层的 `$('#buttons').addEventListener` 还在，页面一加载就抛异常。
- 做法：`read` 看清上下文 → `edit` 精确改 → `node --check` → 跑测试。脚本只能用来**做机械统计/批量改名**，不能用来改结构。

### 2. 改完立刻实测，不靠推断
- 我先后猜错过三次，全是"量了才对"：
  - 「盖板挡住文字」：我以为只是布局挤，实测是**盖板窗口（87×26@916,11）永久置顶、正好压在岛窗口（576×195@672,0）正中**；
  - 「游标尺刻度挤在左边」：实测是**首次渲染时容器还在布局中间态（270px，最终 524px）**，刻度只画了 9 个；
  - 「惯性」：第一版限速 32px/帧，实测小甩一下滑 320px（≈17 分钟），完全不是"滑一下"。
- 做法：能测量就不要描述。窗口量 `getBounds`，内容量 `getBoundingClientRect`，刻度数量 DOM。

### 3. 先量化，再动手
- 有效量化的例子：窗口重叠 87×26、刻度 9→29、秒级区 176px→**440px**（改宽后才能真正停在秒上）、窗口高 174→144。

### 4. 断言要测「行为」，不要测「精确像素」
- 惯性上线后，所有"拖 120px 应该正好落在 15 分钟"的断言全废 —— 松手后还会多滑一段。
- 改法：测「**显示的值 → 实际计时** 精确对齐」（`游标 56s → totalMs=56000`），而不是测像素距离。
- 附带教训：标签只显示到分钟（"30 分钟"可能是 1800~1859 秒），**拿显示值反推**去断言会误判。

### 5. 稳定失败不许当波动放过
- 一次 `T10 全屏时通知被静默` 失败，我没跳过：查出来是它依赖探针的**像素级全屏判定**，在无显示器环境下不稳。改成直接置位 `fullscreen` 只测静默逻辑本身。

### 6. 「预存失败」必须用干净基线对照证明
- 用 `git stash` 跑干净 v3.0.6，证明 T21/T22/T23 在**原始代码上同样失败**（无 GPU/显示器），才敢说与本次改动无关。日志留档 `shots/baseline-t21-t23.txt`。

### 7. 不为了迎合有缺陷的检查器去改产品代码
- 交付检查的 `encoding-utf8` 按**字节**截断到 64KB，第 65536 字节正好是汉字「设」的首字节 `0xe8` → 判为非法。全文件用 .NET `UTF8Encoding(throwOnInvalidBytes)` 和 Node `TextDecoder(fatal)` 都是 VALID。
- 做法：换一个边界安全的主文件 + **在证据里如实写明这是检查器假阳性**。绝不为了让它变绿而在产品文件里塞 47 字节填充。

### 8. 连带影响要一并处理（改一处 = 想清它的上下游）
- 移除大窗口按钮 → 它**穿透**的恢复要一起做（那些按钮当初就是为可点击才改成非穿透的）；
- 删除元素 → 引用它的监听器/查询要一起删；
- 改刻度单位（分 → 秒）→ 下限、标签格式、`timerStart` 的 `Math.min(60s)` 全都要跟着改。

### 9. 临时脚手架用完即删
- `diag-fit.js`、`diag-cover.js` 及其 argv 钩子，验证完立刻删干净，并 `grep` 确认无残留。

### 10. 不可逆动作必须明确同意
- 发布 release、关机、改已发布版本的 tag —— 一律等明确指令。别人说"先不用发布了"，就真的不动 GitHub。

### 11. 测试必须在后台、不可见
- 跑一次自检会在桌面上反复闪出小岛/配置页/盖板，很打扰。
- 做法：`src/main/quiet.js`，非用户场景（`--test` / `--diag-*` / `--shot*` / `--glass-lab`）下窗口 `setOpacity(0)` + 不占任务栏。
- **关键取舍**：用 `setOpacity(0)` 而不是 `show:false` —— 窗口必须保持"已显示"才会正常布局绘制，否则几何断言、`executeJavaScript`、`capturePage` 全坏。已实测截图在这种情况下仍正常出图。

### 12. **先给改动内容，确认后再动手**（用户明确要求）
- 顺序固定：**① 说清改什么 → ② 等确认 → ③ 才改**。不要"边想边改"。
- 方案要说全这五件事：**改哪个文件的哪一段** / **具体改成什么** / **行为会怎样变化** / **会影响什么（连带风险）** / **怎么验证**。
- 例外：用户明确说"直接改"时才可以跳。不可逆动作（发布、关机、改历史 tag）本来就还要单独确认。

---

## 二、产品原则（这软件该怎么长）

### 1. 灵动岛是**唯一常态**，其它形态都是例外
- 程序初始状态必须是灵动岛。
- **计时坞只由长按打开**：不常驻、不持久化、不因任何自动条件弹出。
  （踩过的坑：长按把它写成 `manual.mode='dock'` 并持久化 → 以后每次启动都停在计时坞。）

### 2. 信息不重复
- 灵动岛上已经有「秒表 + 剩余时间」，盖板就**不再**显示剩余时间。
- 任何"同一件事说两遍"的显示都是缺陷。

### 3. 一切形态都必须为「永久置顶的盖板」让位
- 盖板（挖孔屏避让）是独立窗口、永久置顶、永不移动，实测压在岛窗口顶部正中。
- 因此**内容必须避让**：创建页顶部留 52px 让整块落在盖板下方；不能靠"盖板透明"来回避。
- 推论：把内容**居中**是危险的默认值 —— 居中正好送进盖板底下。收缩窗口高度时要同时把容器改成顶部对齐，否则居中逻辑会把内容重新顶上去。

### 4. 盖板的内容规则只有一条主线
> **只要不是灵动岛（strip）状态，盖板一律不显示任何内容。**
- 不要在这里开"例外"（我开过"计时中"和"设置中"两个例外，被连着纠正两次）。

### 5. 控件要够大、够好点、看得出含义
- 关键按钮 54×54（远超 WCAG 2.5.5 的 24px、Apple HIG 的 44pt）；
- 小按钮点击目标从 21px 提到 27px；
- 不用字体里的 ✓ ✗ 字形（不同字体粗细比例不一，画出来歪），改用 **SVG 等宽描边、方头方角**；
- 不加"看不出含义"的图标（产品规范）。

### 6. 刻度 = 分段坐标，兼顾精度与手感
- 5 秒~1 分钟：每格 **2 秒**（这一段必须够宽，440px，否则一甩就冲出去）；
- 1 分钟~6 小时：每格 **2 分钟**（8px/分钟，够精细）；
- 超过 6 小时：**对数** ×1.25（大时长不用拖到手酸）；
- 最短 5 秒；**向上不封顶**（无限拖拽）；
- 中间一条**固定指示线**，刻度尺在它下面滑；松手有**惯性**，按下即停；
- 刻度是虚拟生成的，**`from` 不能钳到 0** —— 负格数就是秒级区，钳掉那里就一根刻度都没有。

### 7. 默认不打扰
- 自动弹大窗口默认**关**（`zoomIdleSec: 0`）；
- 接管系统通知加节流（默认 5 秒），**全屏授课时静默**；
- 大窗口是纯展示（鼠标穿透），不挡桌面操作。

### 8. 破坏性操作要可撤销
- 删除课表/节假日/任务/取消倒计时 → 统一「删除后 5 秒内可撤销」，而不是阻断式确认（不打断连续操作）。
- 危险按钮的文案要写**真实后果**（"删除此事件"而不是含糊的"取消倒计时"）。

### 9. 手动优先，自动行为不得覆盖用户的选择
- 计时开始不再 `setManual('auto')`（那会悄悄改掉用户设的固定/隐藏/驻留模式）；
- 手动模式：`auto / pinned / zoom / hidden` —— **计时坞不再是其中之一**。

### 10. 空态是合法的
- 事件删光就是真的空（渲染层有空态提示 + 托盘入口），不要"自动补一条默认事件"——那会让用户觉得"删除不生效"。

### 11. 左右允许不对称
- 灵动岛的左右**不必强求对称**：内容可以偏向一侧，背景/窗口也可以不对称地长。
- 这条是对"居中默认值"的松绑（见原则 3）。有明显收益时优先用不对称，不要为了对称白丢空间。

### 12. 文档与代码必须一致
- 反例：版本说明写着"快捷按钮图标已改成文字"，代码里其实还是 emoji、后来还被 `opacity:0` 隐藏成死 UI。
- 反例：配置页写"0 = 只用上面那个"，代码里 0 其实是"永不自动放大"。

---

## 三、反面清单（我做错过的，按代价排序）

| # | 错误 | 代价 |
|---|---|---|
| 1 | 把用户"除了计时坞的计时事件"理解成开例外 | 盖板规则被连着纠正两次 |
| 2 | 脚本化片段删按钮，切在半个 `});` 上；同一次删元素却漏了监听器 | 渲染层会直接抛异常 |
| 3 | 惯性第一版限速过宽（32px/帧 ≈ 320px） | 手感完全不对，得回炉 |
| 4 | 秒级区只给 176px 宽 | 有惯性后根本停不到秒上，功能等于没有 |
| 5 | 断言写"精确像素落点" | 惯性一上线全废，得重写 |
| 6 | 把内容居中放进创建页 | 正好被盖板压住 |
| 7 | 断言依赖无显示器环境下的像素判定 | T10 偶发失败 |
| 8 | 拿标签（只精确到分钟）反推校验 | 误判 |
