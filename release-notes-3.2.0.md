# Smart Counter Island 3.2.0

## 灵动岛细条：宽度随内容自适应

- 细条不再是一块固定 116px 宽的额度：**内容多宽就多宽**（左瓣 + 右瓣 + 行内间距），两侧各留 10px 呼吸空间。
  以前事件名短的时候，两侧会各空出十几二十像素，看起来像没对齐。
- 宽度变化不会挡住文字：无论怎么切换事件、内容多长，文字都不会滑进摄像头盖板那一块。

## 摄像头盖板：现在也能操作

- 盖板（屏幕顶部那块黑色胶囊，压在灵动岛上）此前会**吃掉**落在它上面的鼠标 / 触摸 —— 在它上面下滑、长按都没有反应。
  现在盖板与灵动岛手势完全一致：**下滑放大、上滑收起、单击收起、双击打开配置、长按进计时坞**。
- **修好「长按盖板没反应」**：旧代码把手势起点记错位置且从不重置，手指微动几像素就会把长按取消掉。
- 长按反馈变明显：按下去时盖板与灵动岛**同时放大**（按要求不加发光）。

## 计时坞：设置时长的手感重做

- **游标尺改成纯线性**（去掉了 6 小时以上的对数坐标）；上限 **6 小时**。
- 松手后的滑行改成**恒定减速度** —— 甩多快滑多远可预期，不再急停。
- 手感做进设置：**计时坞惯性 = 急促 / 标准 / 平缓**（默认标准）。平缓最绵长，急促最干脆。
- 划到头（5 秒 / 6 小时）时边界外**不再画刻度** —— 以前会画出一排纹丝不动的刻度，看着像卡住。

## 新增：超过 6 小时的「自定义」

- 刻度一直往左滑到 **6 小时**上限时，右端会出现「**自定义…**」；点开输入分钟数，最长 **24 小时**。
  适合连堂这种超过 6 小时的时长。

## 计时坞：左侧时间一长，绿色指示线不再跑位

- 以前选中的时间文字变长（「15 分钟」→「8 小时 20 分」）会把刻度条挤窄，中间那条绿色指示线跟着左右移动。
  现在左侧时间占用**固定宽度**，指示线永远在正中。

## 设置页更简单

- 删掉「**自动放大门槛**」这一项：它与「闲置多少秒后放大到大窗口」重复。现在这一项自己就是开关 ——
  **0 = 永不自动放大（默认保持不变）**，填了秒数就闲置到点自动弹。旧配置里启用过的会自动并过去，不用手动改。
- 「边缘高光强度」默认值从 100% 调到 **5%**（原默认太刺眼）。

## 事件面板简化 + 中高考预设

- 面板里去掉「图标」与「强调色」两个输入（内部改用默认值，灵动岛上的显示不受影响），「名称」改叫「**简称**」。
- 新增「**高考**」「**中考**」一键预设：点一下自动填好简称与**下一个**对应日期（高考 6 月 7 日、中考 6 月 13 日），日期过了自动取明年。

## 计时坞展开后不再响应长按

- 坞已经打开时长按不再有任何动作（防误触）；刻度尺拖动、✓ / ✗ 按钮不受影响。

## 移除两个全屏授课形态

- 删掉「**右上角角落卡片**」与「**顶部进度条**」。配置页「全屏授课时」只剩两项：**完全隐藏**（默认）/ **保持灵动岛小条**。
- 旧配置里选过这两个的话，升级后自动落到「保持灵动岛小条」，不需要手动改。

## 其它

- 配置页新增「计时坞惯性」；「透明度」随形态精简为三项（灵动岛 / 横幅 / 倒计时窗口）。
- README 同步更新。