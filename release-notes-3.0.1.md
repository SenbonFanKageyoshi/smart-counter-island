# Smart Counter Island 3.0.1 版本说明

> 发布日期：2026-09-23 · 断言总数 273（`electron . --test` / `--test-fast` 全绿）
> 产物：`release/SmartCounterIsland-Setup-3.0.1.exe`（NSIS 安装版）· `release/SmartCounterIsland-3.0.1-portable.exe`（免安装便携版）

在 3.0.0 基础上按反馈做的三项调整，都是"减法"。

## 一、去掉横幅模式（形态只剩：细条 ↔ 大窗口）

- 原来有三档形态：细条（灵动岛）→ **横幅（420×64 中间态）** → 大窗口；现在**横幅整体去掉**。
- 新的形态流转：
  - **启动即细条**（以前默认是横幅）；
  - 闲置到 `max(闲置多少秒后放大, 自动放大再等多少秒)` 且「允许倒计时窗口」打开 → **直接放宽成大窗口**（不再先横幅再大屏两段式）；
  - 有操作（点击 / 上滑 / 悬停离开）→ 收回细条；操作后冷却期内不自动放大；
  - 手动「大窗口驻留」若关掉「允许倒计时窗口」→ 收回细条（原来退化为横幅）。
- 横幅状态仍保留在内部（仅诊断/自检可达），所以收缩路径、禁区避让、圆角过渡这些几何自检一条没丢。
- 配置页文案同步：`闲置多少秒后放大到大窗口` / `自动放大再等多少秒（取两者较大值）`。

## 二、去掉所有图标内容（纯文字）

- 岛内不再画任何图标/emoji：细条、大窗口、通知、进度条里的图标全部去掉，只留**数字 / 单位 / 事件名 / 天气文字**。
- 天气 chip 也不再画天气图标（太阳/云/雨/雪/雷/雾），只留 `19° 小雨` 这样的文字（整岛雨雪特效保留）。
- 传感器盖板上的天气同样只有文字。
- 配置页顶栏与「关于」页的应用图标也去掉了。
- **几何不变**：挖孔屏 split 布局左边那一瓣保留一块**等宽留白**（18px）——直接删掉会让右侧数字顶进禁区带、被兜底裁切；实测禁区对齐与以前逐像素一致（`l:[28,46,18] gap:[51,164,113] r:[169,201,32]`）。

## 三、盖板显示天气时，岛内不再重复显示

- 当「天气位置 = 盖板上」时，天气已经在中间那块盖板上显示 → **岛内任何形态（含大窗口）都不再画天气 chip**，避免同一份天气出现两次。
- 其他位置（左端/右端）行为不变。

## 四、字体平衡 + 填补传感器避让造成的空白

- 去掉图标后，细条左瓣（原来放 emoji 的位置）变成一块黑空 —— 现在把**事件名**填进去：`[期末考] [中间禁区] [10 日]`，左右各占一侧，视觉平衡、不再浪费那块黑。
- 字体做了平衡：事件名 12px/600（限宽 4.2em，超出省略号）· 天数 13.5px/800 · 单位 12.5px/600 —— 一眼先看到天数，再看是什么事件。
- **为什么名字要限宽**：挖孔屏下空隙宽度 = 禁区 87 + 两侧槽位 26 = 113，整行虽是居中，但左右瓣宽度差会把这个空隙整体推偏；左瓣超过约 58px 就会让空隙盖不住禁区带（有兜底裁切，但会难看）。所以名字限宽 + 省略号，实测 `l:[19,55,36] gap:[60,173,113] r:[178,210,32]`：禁区 69.5~156 完全落在空隙内，两侧内容都不越界。
- 自适应：没有事件名时左瓣退成一小段留白，不会一边倒。

## 自检

- **273 项断言全绿**（`electron . --test` / `--test-fast`）。
- 随本次改动更新的断言：状态机（闲置→大窗口、两个闲置门槛取较大值、冷却期维持灵动岛、禁用大屏→细条）、T9 冷却期前提修正、T5 探针就绪改为轮询（探针首次握手是异步的，紧随其后的圆角/穿透/亮度断言才是真凭据）。
- 走查截图：`shots/init-before-expand.png`（细条只有 `10 日`，无图标）、`shots/island-weather-strip.png`（`26554 天` + 文字版天气 `19°`）。
