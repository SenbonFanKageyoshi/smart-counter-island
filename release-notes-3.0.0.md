# Smart Counter Island 3.0.0 版本说明

> 发布日期：2026-09-22 · 断言总数 266（`electron . --test` / `--test-fast` 全绿）
> 产物：`release/SmartCounterIsland-Setup-3.0.0.exe`（NSIS 安装版）· `release/SmartCounterIsland-3.0.0-portable.exe`（免安装便携版）
> 从 2.x 升级：设置文件自动补新分节，旧配置照常可用。

3.0 是把「教室大屏倒数日」扩成一整套**教室内信息终端**：天气（含挖孔屏盖板上的自定义内容）、传感器避让几何、桌宠走动手感、以及整个设置页的重做。

## 一、天气系统（全新）

| 能力 | 说明 |
|---|---|
| 数据源 | Open-Meteo（免密钥、零配置）：地理编码 + 当前天气 + 逐小时 + 7 天预报，`timezone=auto`；零运行时依赖（Node 内置 https） |
| 城市 | 手填城市名 → 自动地理编码并缓存经纬度；「解析城市」先确认「城市 · 省/州 · 国家」；「自动定位」按公网 IP 一键填（仅点击时执行） |
| 刷新与降级 | 默认 30 分钟（最小 10）；8 秒超时；失败保留旧数据 + 状态行标注「数据已过期」，重试退避到 3 倍间隔，绝不弹崩溃框 |
| **常驻灵动岛** | 细条 / 横幅 / 大卡片都显示：细条右缘或左缘的紧凑 chip（图标 + 温度）；挖孔屏下自动加宽出天气槽，两瓣仍紧贴禁区 |
| **天气位置可调** | 右端（默认）/ 左端 / **盖板上**（画在中间那块传感器盖板窗口里，细条不占位） |
| 图标动画 | 晴（光线旋转）/ 多云·阴（云飘）/ 雨（雨丝）/ 雪（雪花）/ 雷（闪）/ 雾（流动）；纯 CSS transform/opacity，强度 0–200，尊重系统「减少动效」 |
| 提醒特效 | 天气提醒弹出时**整条灵动岛**播对应特效（雨丝 / 雪花 / 晴空 / 云影 / 雷闪 / 雾带），层数按强度缩放 |
| 指定时间提醒 | 天气页独立规则表：`时间 + 星期（每天/仅一次/工作日）+ 内容（当前/今日/明日/降雨/降温·高温）`；20 秒轮询匹配到分钟、同分钟不重复、「仅一次」触发后自删 |
| 阈值与高亮 | 高温阈值（35℃）、降温阈值（8℃）、降雨前瞻（6 小时）、关键词红色高亮（雨/雪/大风/降温/高温，可自定义） |
| 「试一条」 | 配置页一键按所选类型弹示例，确认文案与动画 |

## 二、盖板自定义内容（自定义文字 + 程序变量）

- 中间那块传感器盖板上可画**自定义文字**：模板 + **16 个程序变量** —— `{emoji}` `{name}` `{days}` `{unit}` `{sub}` `{date}` `{count}` `{time}` `{sec}` `{week}` `{subject}` `{next}` `{min}` `{temp}` `{weather}` `{city}`。
- 配置页有**变量胶囊**（点一下插到光标处）+ **实时预览**（按当前真实事件/课表/天气渲染）；未知变量渲染成空串。
- 例：`{name} 还有 {days}{unit}` →「高考 还有 26555天」；`{time} {temp}° {weather}` →「08:30 19° 中雨」。
- 规则：模板非空时盖板只画文字（宽度全给文字）；模板留空时盖板自动显示天气 chip。

## 三、传感器避让（挖孔屏）修好并加固

- **冷启动盖板不创建**（老 bug）：启动链路里没人调用 `syncSensorCover()`，挖孔盖板只有改设置才会冒出来 → 现在建窗后立即同步 + `tick()` 自愈重建。
- **收缩回灵动岛时文字消失**：状态通知原来下发「当前窗口坐标」，动画中渲染层按横幅的旧坐标算内容让位，把两瓣推出可视区 → 改为下发**目标几何**。
- **启动后没有文字**：两瓣被 `flex-shrink` 压成 11px/0px（窗口刚创建还是细条初始尺寸）→ 两瓣改 `flex: 0 0 auto` 永不压缩，动画收尾强制重排重绘。
- 几何契约（保持）：细条宽 = 116 + 禁区 + 左右槽位（+ 天气槽）；盖板与禁区取整后完全重合、永不移动；内容永不侵入禁区。

## 四、桌宠移动逻辑优化

| 问题 | 修法 |
|---|---|
| 逐帧步进 + 隔帧 0.6 倍系数 → 帧率一变速度就变、一顿一顿 | 改按**真实时间**步进（`speed × 48 px/s`）；挂起后单帧位移封顶 80ms |
| 贴屏幕边缘时窗口被夹住、渲染层却按请求值补偿 → 原地抽搐 | 主进程回报**实际** dx/dy；窗口没动就转身 + 停顿 320ms |
| 固定用主屏工作区 → 副屏上的桌宠会被拉回主屏 | 改用 `getDisplayMatching(窗口)` |
| 每平移一格写一次设置 | 位置写盘 **1.2s 防抖**，退出前兜底落盘 |

## 五、设置页重做：液态玻璃 + 布局重构

- **真液态玻璃底**：配置窗口改无边框 + Win11 亚克力（`setBackgroundMaterial('acrylic')`，系统把背后的真实桌面模糊/提色）；页面再叠半透明玻璃面板（`backdrop-filter: blur(22px) saturate(1.5)` + 高光描边 + 柔和投影）。不支持亚克力时自动退回深浅玻璃底。
- **布局**：顶部通栏（logo + 版本 + **设置搜索** + 自绘窗口按钮：拖动 / 双击最大化 / 最小化 / 关闭）＋ **左侧分组导航**（教学 / 外观 / 行为与系统）＋ 右侧**两列字段卡**（窄窗自动回落单列）；窗口 900×820。侧栏不带装饰图标（纯文字，选中态用高光区分）。
- **盖板内容不再藏在折叠区**：以前「盖板自定义内容」被放在「显示高级参数」的折叠块里，默认看不见 → 现在独立成 **「传感器盖板内容（挖孔屏中间那块黑上显示什么）」** 分区，默认可见（含输入框、16 个变量胶囊、字号、实时预览），「天气 → 天气位置」旁边也加了指引。
- **设置搜索**：实时过滤当前页（命中蓝框、未命中隐藏、孤立分组标题一起隐藏）；当前页没命中自动跳到第一个命中页；Esc / ✕ 清空。
- 其他：`textarea` 统一玻璃皮肤（修掉暗色下白底）、按钮/输入框/下拉重做（聚焦蓝环、主按钮渐变高光）。

## 六、工程与自检

- **265 项自动化断言**全绿（`--test` 与 `--test-fast` 同一套）：覆盖状态机、玻璃管线（含 GPU 链路与帧率）、通知、时间表、定时任务、天气（映射/解析/文案/去重/离线降级/细条常驻几何）、盖板自定义内容、壁纸、屏幕挖孔避让、桌宠移动、设置页外壳。
- 自检结果与异常堆栈都会写进 `%TEMP%\sci-test-last.log`（便携版拿不到控制台输出时也能判定）。
- 诊断随手可跑：`--shot-init`（启动初始化快照 + 像素统计）、`--shot-collapse`（收缩路径采样）、`--shot-weather`（天气 chip/特效/盖板截图）、`--shot-config`（设置页逐页截图）、`--shot-strip`、`--smoke`、`--perf`。
- 零运行时依赖 / 零原生模块不变：仍只有 Electron + PowerShell 探针 + WebGL/SVG。

## 七、玻璃实验室窗口 + 边缘色散（为替换「真实模糊」做准备）

- 新增 **玻璃实验室窗口**：设置页「显示效果 → 玻璃效果」旁的 **「预览玻璃效果…」** 按钮（或 `--glass-lab`）打开一块置顶窗口，用**同一张桌面截图**并排渲染 4 张对照卡：
  ① 真实模糊（现状）② 液态玻璃 CPU（现状）③ **＋边缘色散 2px（候选）** ④ ＋色散 5px＋强折射（候选）；
  底部滑块实时调 折射强度 / 折射带宽 / 渗色 / 高光 / 色散 / 模糊（真实模糊卡）——拖到满意点「把这组参数用到小岛」即写回设置。
- **边缘色散**（本次新增的观感项）：R/G/B 三通道各按不同折射量位移，再用 `screen` 合成 → 玻璃边缘出现真玻璃那种红蓝细边。参数 `ui.glassAberration`（px，**默认 0 = 关闭**；0 时滤镜链与旧版逐字节相同，升级后观感零变化）。
- CPU 液态玻璃的观感参数全部参数化并可写回：`ui.refractWidth`（折射带宽）/ `ui.maxRefract`（最大折射位移）/ `ui.bleedOpacity`（渗色）/ `ui.glassGlow`（高光）。
- 工程细节：`liquid-glass.js` 支持**多实例**（岛沿用原有 id 不变；实验室各卡独立 id/滤镜），新增 `aberration` 选项；取屏前自动隐藏实验室窗口 —— 否则会把自己拍进背景，出现「卡片套卡片」的无限递归。
- 断言（T33，6 条）：窗口打开 · 四张对照卡 + 隐藏取屏 · 色散=0 回到单条滤镜链 · 参数写回设置 · **小岛真的用上色散（岛滤镜位移次数 1→3）** · 清零后回到 1；截图 `shots/glass-lab.png`。

## 八、新增「液态玻璃（色散版）」档 + 实验室控件修复

- **新模式 `liquid-ab`：液态玻璃（色散版·边缘红蓝细边）** —— CPU 链路 + **固定 2px 边缘色散**（不跟随「边缘色散」设置项，开箱即用观感一致）；其余六档全部保持原样，随时可切回。
  - 断言：`新模式「液态玻璃（色散版）」= CPU 链路 + 固定色散 (模式=liquid-ab 位移次数=3 data-ab=12，设置里色散=0)` ✅
- **修复玻璃实验室顶部控件点不动**：标题栏是窗口拖动区（`-webkit-app-region: drag`），而拖动区会**吞掉子元素的鼠标事件** —— 里面的「实时背景 / 重新取屏 / 把这组参数用到小岛 / 关闭」都没有点击响应。已给这些可交互子元素显式加 `-webkit-app-region: no-drag`。
  - 断言：`实验室顶部控件可点（no-drag）`（逐个查 `live/refresh/apply/close` 的计算样式）✅
- 自检：**274 项**。

## 升级提示

- 设置文件新增 `weather`、`ui.cameraNotch.text` 等分节，旧配置自动补默认值，行为不变。
- 已移除天气的语音/桌宠播报（`weather.announcePet`）与更早版本的死配置键 `pet.voice`（自动清理）；课堂提醒的桌宠播报（`announceClass`）保留。
- 未做代码签名，Windows 首次运行可能提示「未知发布者」→「更多信息 → 仍要运行」。
