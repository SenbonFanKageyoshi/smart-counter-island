# Smart Counter Island 2.2.0 版本说明

> 发布日期：2026-09-22 · 断言总数 265（`electron . --test` / `--test-fast` 全绿）
> 产物：`release/SmartCounterIsland-Setup-2.2.0.exe`（71.4 MB，NSIS 安装版）· `release/SmartCounterIsland-2.2.0-portable.exe`（71.2 MB，免安装便携版）

## 本次追加（按反馈）

### 设置页重做：液态玻璃 + 布局重构

| 项 | 内容 |
|---|---|
| 真液态玻璃底 | 配置窗口改 **无边框 + Win11 亚克力**（`setBackgroundMaterial('acrylic')`）：窗口背后的真实桌面被系统模糊/提色；页面本身再叠一层半透明玻璃面板（`backdrop-filter: blur(22px) saturate(1.5)` + 高光描边 + 柔和阴影）。不支持亚克力（Win10）时自动退回深/浅色玻璃底（body 背景带 alpha 下限，不会变成全透窗口） |
| 布局重构 | 顶部通栏（logo + 版本 + **设置搜索** + 自绘窗口按钮）＋ **左侧分组导航**（教学：倒计时/时间表/定时任务/天气 · 外观：显示效果/壁纸/桌宠 · 行为与系统：智能行为/高级/关于；带图标与选中高光）＋ 右侧内容区（**两列字段行**：左标签右控件，窄窗自动回落单列） |
| 设置搜索 | 顶栏搜索框：实时过滤当前页字段（命中蓝框、未命中隐藏、孤立的分组标题一起隐藏）；当前页没命中会**自动跳到第一个命中页**（如搜「玻璃」→ 显示效果）；Esc / ✕ 清空 |
| 无边框窗口交互 | 自绘标题栏：按住拖动（主进程按屏幕坐标搬窗口，跟手且不吃点击）、双击最大化/还原、`–` 最小化、`✕` 关闭；`thickFrame` 保留边缘缩放；窗口尺寸放大到 900×820（宽屏两列更舒展） |
| 保留契约 | DOM 契约原样保留（`#tabs .tab[data-tab]`、`#tab-xxx.active`、全部字段 id），265 条自检里所有设置页相关断言（T3/T7/T20/T30/T32）全绿 |
| 断言（T32，3 条） | 侧栏 `flex-direction: column` + 3 个分组 + 10 个标签带图标 + 标题栏玻璃 `rgba(255,255,255,0.74)/blur(22px) saturate(1.5)` + 字段两列 + 圆角；无边框拖动（+60/+40 精确命中）；搜索过滤（命中 4 / 隐藏 103 / 自动跳页 / 清空复原） |
| 走查截图 | 新增诊断 `--shot-config` 逐页截图：`shots/config-{events,schedule,tasks,weather,display,wallpaper,pet,smart,advanced,about}.png` + `config-search.png` |


### 盖板自定义内容：自定义文字 + 程序变量

- 配置页「显示效果 → 传感器避让」新增 **盖板自定义内容**：模板字符串 + **16 个程序变量**，画在中间那块传感器盖板窗口上。
  - 变量：`{emoji}` `{name}` `{days}` `{unit}` `{sub}` `{date}` `{count}` `{time}` `{sec}` `{week}` `{subject}` `{next}` `{min}` `{temp}` `{weather}` `{city}`
  - 例：`{name} 还有 {days}{unit}` → 「高考 还有 26555天」；`{time} {temp}° {weather}` → 「08:30 19° 中雨」；`距 {next} 还有 {min} 分`
  - 配置页变量胶囊一点即插入到光标处；旁边有**实时预览**（走 IPC 用当前真实事件/课表/天气渲染，所见即所得）；未知变量渲染成空串，不报错。
  - 主事件挑选规则与灵动岛一致（置顶优先 → 目标时间最近 → 全过期取第一个），天数取整跟着「日期估算方式」。
- 规则：**模板非空时盖板只画文字**（不再自动叠天气 chip）—— 盖板宽度就等于禁区宽度（约 87px），两样一起放会互相挤成省略号；想要天气就在模板里写 `{temp}° {weather}`。模板留空时盖板自动显示天气 chip（原行为）。
- 实现：纯函数模块 `src/main/cover-text.js`（buildContext / renderTemplate / timeUnits / pickPrimary，依赖注入、可直接单测）；盖板窗口沿用 `sensor-preload.js` 的 `cover:content` 通道，主进程渲染好文本再推下去。
- 断言（T31，10 条）：变量上下文、模板渲染（含未知变量/空模板/缺上下文）、变量清单可枚举、天数取整与渲染层同语义、单位降级、主事件挑选、**盖板端到端**（文字真的画上去 + 模板留空则只画天气）、配置页预览 IPC；截图 `shots/island-weather-cover.png`。

### 天气位置可调：右端 / 左端 / **盖板上**（中间的传感器窗口）

- 新增设置 `weather.pos`（配置页「天气 → 天气位置」）：
  - **右端**（默认）：紧凑 chip 贴细条右缘；
  - **左端**：镜像贴左缘（细条往左多留一格，两瓣与禁区的相对位置不变）；
  - **盖板上**：天气画在**传感器盖板窗口**里（那块永不移动的纯黑胶囊）—— 盖板是独立置顶窗口，所以给它加了 `sensor-preload.js` + `renderer/sensor/sensor.js`，由盖板自己渲染「图标 + 温度」；细条此时**不占位、不画**（不破禁区几何）。
- ⚠️ 说明：盖板正好压在摄像头那块上，选「盖板上」时天气会与摄像头区域重叠 —— 摄像头不参与使用时再选它（这也是它不做默认值的原因）。
- 断言：`天气位置=左侧 (chip 14~63 贴左缘)`、`天气位置=盖板上（细条让位 has=false 宽 229 期望 229；盖板上 chip={"on":true,"temp":"21°","anim":"rain"}）`；截图 `shots/island-weather-cover.png`（87×26 盖板上画出 ☁ 19°）。

### 天气常驻灵动岛

- `weather.showInIsland` 默认改为 **`always`**：细条（右缘紧凑 chip：图标 + 温度）/ 横幅 / 大卡片都显示天气；仍可改「仅横幅·大卡片」或「不显示」。
- 细条几何：挖孔屏下自动加宽一个**天气槽**（88px），chip 绝对定位贴细条右缘 —— 不参与整行居中，所以「两瓣紧贴传感器、中间空隙 = 禁区 + 槽位」的既有几何完全不变；不挖孔时细条 = 116 + 88。
- 断言：`天气常驻细条（宽 317 期望 317；chip=true 紧凑=true 只图标+温度；chip 270~319 右瓣右缘=250 禁区右缘=201）`。

### 去掉天气语音/桌宠播报

- 删除 `weather.announcePet`（配置页勾选项、默认值、提醒回调里对桌宠的调用一并移除）；顺带清理更早版本残留的死配置键 `pet.voice`（设置迁移中删除，无人读取）。
- 课堂提醒的桌宠播报（`announceClass`）保持不变 —— 只去掉天气这条。

### 桌宠移动逻辑优化

| 问题 | 修法 |
|---|---|
| 逐帧步进 + 隔帧 0.6 倍系数 → 帧率一变速度就变、看着一顿一顿 | 改按**真实时间**步进（`speed × 48 px/s`，与旧手感一致）；窗口被挂起后单帧位移封顶 80ms，防跳跃 |
| 到屏幕边缘：窗口被工作区夹住、渲染层却按请求值补偿 → 原地往返**抽搐** | 主进程 `moveStage` 回报**实际** dx/dy；渲染层按实际值补偿，窗口完全没动就**转身 + 停顿 320ms** |
| 固定用主显示器 workArea → 副屏上的桌宠会被拉回主屏 | 改用 `screen.getDisplayMatching(窗口)`，按桌宠所在那块屏的工作区限制 |
| 每平移一格就写一次设置（走路时每一步都落盘） | 位置写盘 **1.2s 防抖**（`savePosSoon` / `flushPos`），退出前兜底落盘 |

- 断言：`舞台平移并记住位置（位置写盘防抖）`、`平移被工作区夹住并回报实际位移（请求 +100000 → 实际 dx=404，再推一次 moved=false）`。


## 新增：天气预报系统（Open-Meteo，免密钥）

| 能力 | 说明 |
|---|---|
| 数据源 | Open-Meteo：地理编码 + 当前天气 + 逐小时 + 7 天预报；零运行时依赖（Node 内置 https），`timezone=auto` |
| 城市 | 手填城市名 → 「解析城市」先确认「城市 · 省/州 · 国家」；「自动定位」按公网 IP 一键填（仅点击时执行，不做后台定位） |
| 刷新与降级 | 默认 30 分钟（最小 10）；8 秒超时；失败保留旧数据 + 状态行标注「数据已过期」，重试退避到 3 倍间隔，绝不弹崩溃框 |
| 岛内显示 | 横幅 / 大卡片内联天气 chip（图标 + 温度 + 文案）；默认「横幅」——细条保持极简、不动既有几何；可选「横幅+细条」或「不显示」 |
| 图标动画 | 晴（光线旋转）/ 多云·阴（云飘）/ 雨（雨丝）/ 雪（雪花）/ 雷（闪）/ 雾（流动）；纯 CSS transform/opacity，强度 0–200，尊重系统「减少动效」 |
| 提醒特效 | 天气提醒弹出时整条灵动岛播对应特效（雨丝 / 雪花 / 晴空 / 云影 / 雷闪 / 雾带），层数按动画强度缩放 |
| 指定时间提醒 | 天气页独立规则表：`时间 + 星期（每天/仅一次/工作日）+ 内容（当前天气/今日概况/明日预报/降雨提醒/降温·高温）` |
| 提醒去重 | 与定时任务同语义：20 秒轮询匹配到分钟，同一分钟不重复；「仅一次」触发后自删 |
| 可调阈值 | 高温阈值（默认 35℃）、降温阈值（默认 8℃）、降雨前瞻（默认 6 小时）、关键词红色高亮（默认 雨/雪/大风/降温/高温） |
| 桌宠联动 | 可选：天气提醒时桌宠也念一遍 |
| 「试一条」 | 配置页一键按所选类型弹示例，确认文案与动画效果 |

首次使用：配置页「天气」→ 勾选启用 → 填城市 → 「解析城市」确认 → 「立即刷新」看状态行 → 加一条「每天 07:00 今日概况」。

## 修复：灵动岛文字看不见（启动后无字 / 收缩瞬间丢字）

两个独立成因，都已修掉：

### A. 启动后小岛没有文字（只有展开大窗口再收起才出现）

- **症状**：冷启动后小岛是黑胶囊、文字不显示；把大窗口展开再收起，文字才冒出来。DOM 里文字一直在、颜色也对。
- **根因**：上一轮为"收缩兜底"给两瓣加的 `min-width:0; flex-shrink:1` 会让它们**被压成 0 宽**。窗口刚创建时还是细条初始尺寸（pill 116px），而禁区空隙是固定的 113px（禁区 87 + 左右槽位 26），只剩 3px 分给两瓣 → 左瓣被压成 11px、右瓣压成 **0px**（数字+单位整块落在里面，被行内 `overflow:hidden` 裁掉），而且之后窗口长到 229px 也不再重排。
  - 实测对照（`--shot-init`）：启动时 `nb-l=11px`、`nb-r=0px`、数字位置 `elementFromPoint` 命中的是父容器；展开收起后 `nb-l=18px`、`nb-r=32px`、命中 `.s-num`。
- **修复**：① 两瓣改回 `flex: 0 0 auto`（**永不压缩**）——空间不够时宁可整行边缘裁一点，也绝不让某一瓣塌成 0；间隙保持固定值，兜底只留在整行（`overflow:hidden` + `max-width:100%`）。② 几何动画结束后强制渲染层 `invalidate()` 重排重绘一次，并用**真实几何**再推一次状态，避免"首次布局按初始更窄尺寸算、之后再也不恢复"。
- **验证**：新增断言「split 两瓣不被压缩、数字在可视区内」（左 18 / 右 25 / 空隙 113，命中 `s-num`）；`--shot-init` 实测启动即 `bright=105`（白字已绘制），与"展开收起后"两张截图哈希完全相同。

### B. 从横幅收缩回灵动岛那一下文字消失

- **症状**：横幅/大卡片收缩成灵动岛时，小岛变成黑胶囊、文字整体不见。
- **根因**：状态通知下发的是**当前窗口坐标**，而窗口正在做 150/550ms 几何动画 —— 渲染层拿"横幅的旧窗口坐标"算内容让位，中间空隙按错误宽度铺开，`justify-content:center` 把两瓣推出可视区，叠上 `#pill{overflow:hidden}` 就是"整块消失"。
- **修复**：状态通知改带**目标窗口几何**（`sendState(target)` → `getStatePayload(target)` → `cameraNotch(state, target)`），动画期间一律按最终尺寸算让位。
- **验证**：断言「收缩用目标几何算让位」（禁区相对 x 77.5 = 期望 77.5）与「收缩回灵动岛全程文字可见」（动画中途采样 12 帧，丢失 0 帧）；诊断 `--shot-collapse` 可一键复现/回归。

## 修复：灵动岛初始化（挖孔盖板冷启动缺失）

- **症状**：配置页开启「传感器避让」后冷启动，那块必须永久盖住摄像头的**纯黑胶囊盖板窗口不会创建**——只有改任意设置或切屏才会冒出来（小岛自己已按 split 布局让开禁区，但镜头那块没有被盖住）。
- **根因**：启动链路里 `island.create()` 之后没有任何地方调用 `applySettings()`，而盖板只在 `syncSensorCover()` 里创建；`tick()` 当时只做 `sensorCover.reassert()`，窗口为 `null` 时是空操作。
- **修复**：① `tick()` 改为调用 `syncSensorCover()`（缺失即重建、几何未变时只做一次 key 比较 + 置顶断言，开销可忽略，可自愈）；② 窗口创建后立刻补一次 `island.syncSensorCover()`，首帧就有盖板。
- **验证**：新增断言「盖板缺失时 tick 自愈重建」；`--shot-init` 诊断实测启动后盖板 1s/2s/3s/5s/8s 始终在位，且与小岛禁区完全重合（禁区 `915.5,11,86.5×26` → 盖板 `916,11,87×26`）。

## 其他改动

- 无倒计时事件时横幅也显示天气（空态不再吞掉天气 chip）。
- 配置页标签栏改为单行不换行 + 横向滚动（第 10 个标签挤爆换行导致「时间表」被拆成三行）。
- 配置页新增「天气」标签（共 10 个）。
- 自检增强：`TEST_OK/TEST_FAIL` 结论与异常堆栈都会写进 `%TEMP%\sci-test-last.log`（便携版拿不到控制台输出时也能判定）。
- 自检去抖动：T9「拖放后误触 tap 被忽略」改为显式钉住时间基准，机器负载高时不再偶发误判。
- 新增诊断：`--shot-init`（启动初始化快照）、`--shot-collapse`（收缩回灵动岛复现/回归）、`--shot-weather`（天气 chip / 特效 / 配置页截图）。

## 升级提示

- 设置文件新增 `weather` 分节，旧配置自动补默认值（`enabled: false`），不影响既有行为。
- 未做代码签名，Windows 首次运行可能提示「未知发布者」→「更多信息 → 仍要运行」。
